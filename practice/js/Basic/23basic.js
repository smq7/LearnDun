function replacePosition(str){
    let newStr = str.substring(str.length-1,str.length) + str.substring(1,str.length-1) + str.substring(0,1);
//   mid_char = str1.substring(1, str1.length - 1);
//   return (str1.charAt(str1.length - 1)) + mid_char + str1.charAt(0);
    console.log(newStr)

}

replacePosition("vadim")